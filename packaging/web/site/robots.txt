# Crawl the site; skip the game's payload.
#
# /play/data/ is ~60 MB of maps, audio and fonts fetched on demand by the
# running game. A crawler walking it gains nothing, and spends crawl budget
# that should go on the five pages that are actually pages.
User-agent: *
Allow: /
Disallow: /play/data/

# A plain-language summary for the crawlers behind AI assistants, at
# /llms.txt. Worth having: chatgpt.com referred more visits to the game
# last month than Bing did.
Sitemap: https://opendoctrines.pages.dev/sitemap.xml
