/* Generated by tools/gen_bindings.py from sdk/abi.json. Do not edit. */
/* Included inside gbx_methods[] in gearbox_py.c. */

/* ---- Assets (1) ---- */
#if GBX_WITH_ASSETS
    {"assetSize", gbxpy_asset_size, METH_VARARGS, "Byte size of one of your own data/ files, or 0 if there is no such asset. Names are relative to data/ and use '/' separators: data/flags/fr.png is \"flags/fr.png\"."},
#endif /* GBX_WITH_ASSETS */

/* ---- Audio (4) ---- */
#if GBX_WITH_AUDIO
    {"play", gbxpy_play, METH_VARARGS, "Play a sound from your own mod's assets. `path` is relative to your mod root; a path outside it is refused rather than resolved. Volume is 0..1 and is multiplied by the player's ow"},
    {"stop", gbxpy_stop, METH_VARARGS, "Stop a sound this mod started. A handle belonging to another mod, or one that already finished, does nothing."},
    {"setVolume", gbxpy_set_volume, METH_VARARGS, "Change the volume of a playing sound, 0..1, again scaled by the player's setting."},
    {"isPlaying", gbxpy_is_playing, METH_VARARGS, "Whether that handle is still making sound."},
#endif /* GBX_WITH_AUDIO */

/* ---- Content (5) ---- */
#if GBX_WITH_CONTENT
    {"contentAdd", gbxpy_content_add, METH_VARARGS, "Add or replace one entry in a catalogue. kind 0 doctrine, 1 research, 2 troop type, 3 artillery, 4 district law. mode 0 HOLLOW, 1 PERSIST.  The definition is the SAME JSON the game"},
    {"contentCount", gbxpy_content_count, METH_VARARGS, "How many entries of this kind YOU have added."},
    {"contentIdAt", gbxpy_content_id_at, METH_VARARGS, "The id of your entry at index within a kind, sorted. Two-call sizing."},
    {"contentOwnerOf", gbxpy_content_owner_of, METH_VARARGS, "Which mod owns an id in a catalogue, or empty if nobody does. Lets a mod check whether the content it is about to add already exists -- including content another mod added, which i"},
    {"contentRemove", gbxpy_content_remove, METH_VARARGS, "Remove one of your own entries. False if it was not yours."},
#endif /* GBX_WITH_CONTENT */

/* ---- Core.Protected (5) ---- */
#if GBX_WITH_CORE_PROTECTED
    {"processBytes", gbxpy_process_bytes, METH_NOARGS, "Resident memory the whole game is using, in bytes. 0 where the platform does not report it -- Windows and the web build both return 0 today, and 0 means UNKNOWN rather than 'no mem"},
    {"imageBytes", gbxpy_image_bytes, METH_NOARGS, "How large the game's own executable is on disk, in bytes. 0 if it cannot be determined. Useful to a mod that reports build size or checks it is running against the build it expects"},
    {"modCount", gbxpy_mod_count, METH_NOARGS, "How many mods are INSTALLED, enabled or not. A compatibility checker needs to see the mod it conflicts with even when that mod is switched off, because switching it on is what brea"},
    {"modId", gbxpy_mod_id, METH_VARARGS, "The installed mod's manifest id -- the stable one, safe to compare. Two-call sizing: call with cap 0 to learn the length, allocate, call again."},
    {"modName", gbxpy_mod_name, METH_VARARGS, "Its display name, which is for showing a player and NOT for matching on: it is author-chosen, may be translated, and two mods may share one. Match on mod_id."},
#endif /* GBX_WITH_CORE_PROTECTED */

/* ---- Country (9) ---- */
#if GBX_WITH_COUNTRY
    {"fieldAdd", gbxpy_field_add, METH_VARARGS, "Declare a field on every country. mode 0 HOLLOW, 1 PERSIST; type 0 number, 1 text.  PERSIST is written into the save and read back. HOLLOW is not: the mod redeclares it on every lo"},
    {"fieldRemove", gbxpy_field_remove, METH_VARARGS, "Forget one of YOUR fields and every country's value for it. Returns whether it existed. A mod cannot remove another mod's field: fields are keyed by (mod, name), so two mods may bo"},
    {"fieldHas", gbxpy_field_has, METH_VARARGS, "Whether you have declared this field AND own it right now. False for a field read back from a save whose mod is not loaded -- such a field is inert, though its values are kept."},
    {"fieldCount", gbxpy_field_count, METH_NOARGS, "How many fields YOU have declared. Not how many exist: another mod's fields are not yours to enumerate."},
    {"fieldName", gbxpy_field_name, METH_VARARGS, "The name of your field at index, sorted by name so the order does not shift between runs. Two-call sizing."},
    {"setNumber", gbxpy_set_number, METH_VARARGS, "Set a country's value for one of your NUMBER fields. Refused if the field is text, was never declared, or belongs to a mod that is not loaded."},
    {"getNumber", gbxpy_get_number, METH_VARARGS, "A country's value, or 0 when the field or the country has none. 0 is a real value too, so a mod that needs to tell unset from zero should keep its own sentinel."},
    {"setText", gbxpy_set_text, METH_VARARGS, "Set a country's value for one of your TEXT fields."},
    {"getText", gbxpy_get_text, METH_VARARGS, "A country's text value, or empty. Two-call sizing."},
#endif /* GBX_WITH_COUNTRY */

/* ---- Diplomacy (5) ---- */
#if GBX_WITH_DIPLOMACY
    {"atWar", gbxpy_at_war, METH_VARARGS, "1 if the two countries are at war. Relations are symmetric, so the argument order does not matter. 0 for unknown countries or for a country with itself."},
    {"allied", gbxpy_allied, METH_VARARGS, "1 if the two countries are allied."},
    {"nonAggression", gbxpy_non_aggression, METH_VARARGS, "1 if the two countries have a non-aggression pact."},
    {"guaranteed", gbxpy_guaranteed, METH_VARARGS, "1 if the first country guarantees the second."},
    {"proposeWar", gbxpy_propose_war, METH_VARARGS, "PROPOSES a declaration of war, and returns 1 only if the game accepted it. It is routed through the same code path any other actor uses, so guarantee chains and war consequences fo"},
#endif /* GBX_WITH_DIPLOMACY */

/* ---- Economy.Read (11) ---- */
#if GBX_WITH_ECONOMY_READ
    {"countryIncomeGross", gbxpy_country_income_gross, METH_VARARGS, "Income per turn before upkeep."},
    {"countryIncomeNet", gbxpy_country_income_net, METH_VARARGS, "Income per turn after army and navy upkeep. Negative means the treasury is draining."},
    {"countryArmyUpkeep", gbxpy_country_army_upkeep, METH_VARARGS, "What the standing army costs per turn."},
    {"countryNavyUpkeep", gbxpy_country_navy_upkeep, METH_VARARGS, "What the fleet costs per turn. Ships a country is not using still cost this, which is what makes scrapping a real decision."},
    {"countryIsBankrupt", gbxpy_country_is_bankrupt, METH_VARARGS, "Whether a country is currently bankrupt."},
    {"provinceIndustryLevel", gbxpy_province_industry_level, METH_VARARGS, "Industry level, 0..10."},
    {"provinceIndustrySpecialization", gbxpy_province_industry_specialization, METH_VARARGS, "What this province's industry specialises in, or an empty string for none. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length eithe"},
    {"provinceResource", gbxpy_province_resource, METH_VARARGS, "How much of a resource a province holds, 0..100. `which` is one of \"oil\", \"gold\", \"rubber\", \"gemstones\", \"metal\"; anything else reads 0."},
    {"countryExpenses", gbxpy_country_expenses, METH_VARARGS, "What this country spent last turn, in total. The same figure its profile publishes and the economy screen draws."},
    {"countryNationalValue", gbxpy_country_national_value, METH_VARARGS, "What the whole country is worth: every industry level, fort, port and division at what it cost to raise. A stock, where the income figures are flows."},
    {"countryPopulation", gbxpy_country_population, METH_VARARGS, "How many people live in this country."},
#endif /* GBX_WITH_ECONOMY_READ */

/* ---- Economy.Write (1) ---- */
#if GBX_WITH_ECONOMY_WRITE
    {"setProvinceIndustryLevel", gbxpy_set_province_industry_level, METH_VARARGS, "Set a province's industry level, clamped to 0..10. This writes the built level directly and does not charge for it -- it is a scenario-authoring tool, not a build order."},
#endif /* GBX_WITH_ECONOMY_WRITE */

/* ---- GameState.Read (10) ---- */
#if GBX_WITH_GAMESTATE_READ
    {"turnNumber", gbxpy_turn_number, METH_NOARGS, "The current turn. 0 when no world is loaded."},
    {"countryCount", gbxpy_country_count, METH_NOARGS, "How many countries exist. 0 when no world is loaded. Rebel factions are not included."},
    {"countryAt", gbxpy_country_at, METH_VARARGS, "The country at index in [0, country_count). Returns GEARBOX_INVALID (0xFFFFFFFF) if out of range. Ordering is stable within a turn but not across turns."},
    {"countryName", gbxpy_country_name, METH_VARARGS, "Two-call sizing. Writes at most cap bytes of UTF-8 and returns the FULL length. Call with cap 0 to size, then again to fill. A return greater than cap means truncation, not failure"},
    {"countryTreasury", gbxpy_country_treasury, METH_VARARGS, "Treasury balance. 0 for an unknown country."},
    {"countryProvinceCount", gbxpy_country_province_count, METH_VARARGS, "How many provinces the country owns. 0 for an unknown country."},
    {"provincePopulation", gbxpy_province_population, METH_VARARGS, "Population of a province. 0 for an unknown province."},
    {"provinceOwner", gbxpy_province_owner, METH_VARARGS, "Owning country, or GEARBOX_INVALID if unowned or unknown."},
    {"countryExists", gbxpy_country_exists, METH_VARARGS, "Whether a country id names a country that exists. A mod holding an id from its own storage, a save, or a previous turn has no other way to ask before using it -- a country can be a"},
    {"provinceExists", gbxpy_province_exists, METH_VARARGS, "Whether a province id names a province that exists. Same reason as country_exists: a stored id needs a validity check that is not 'iterate every province and compare'."},
#endif /* GBX_WITH_GAMESTATE_READ */

/* ---- GameState.Write (4) ---- */
#if GBX_WITH_GAMESTATE_WRITE
    {"setCountryTreasury", gbxpy_set_country_treasury, METH_VARARGS, "Sets a country's treasury outright. Returns 1 on success, 0 if the country is unknown or the value is not finite and within +/-1e12 -- NaN or infinity would silently poison every l"},
    {"addCountryTreasury", gbxpy_add_country_treasury, METH_VARARGS, "Adds to a country's treasury. Usually what you want instead of set: it composes with whatever the economy did this turn. Refused (0) if the result would leave the sane range."},
    {"setProvinceOwner", gbxpy_set_province_owner, METH_VARARGS, "Transfers a province to another country. Routed through the same code the game's own ceasefires use, so the province, the ownership lookup, the per-pixel country map and both count"},
    {"setProvincePopulation", gbxpy_set_province_population, METH_VARARGS, "Sets a province's population. Returns 0 if the province is unknown, the value is negative, or it exceeds 100 billion. The game stores population in two places -- a map and a dense "},
#endif /* GBX_WITH_GAMESTATE_WRITE */

/* ---- Map (13) ---- */
#if GBX_WITH_MAP
    {"width", gbxpy_width, METH_NOARGS, "Width of the province map in pixels. 0 when no world is loaded."},
    {"height", gbxpy_height, METH_NOARGS, "Height of the province map in pixels. 0 when no world is loaded."},
    {"provinceCount", gbxpy_province_count, METH_NOARGS, "How many provinces the loaded map has. 0 when no world is loaded."},
    {"provinceAt", gbxpy_province_at, METH_VARARGS, "Province handle at an index in [0, province_count). Returns GEARBOX_INVALID if out of range. The order is stable across runs, unlike the game's internal storage, so an index is saf"},
    {"provinceName", gbxpy_province_name, METH_VARARGS, "The province's name. Two-call sizing: returns the full length and writes at most cap bytes. Empty for an unknown province."},
    {"provinceCenterX", gbxpy_province_center_x, METH_VARARGS, "X pixel coordinate of the province's centre. 0 for an unknown province."},
    {"provinceCenterY", gbxpy_province_center_y, METH_VARARGS, "Y pixel coordinate of the province's centre. 0 for an unknown province."},
    {"provinceIsLand", gbxpy_province_is_land, METH_VARARGS, "1 if the province is land, 0 if it is sea or unknown. Sampled at the province centre."},
    {"provinceNeighborCount", gbxpy_province_neighbor_count, METH_VARARGS, "How many provinces border this one. 0 for an unknown province."},
    {"provinceNeighborAt", gbxpy_province_neighbor_at, METH_VARARGS, "The bordering province at an index in [0, province_neighbor_count). GEARBOX_INVALID if out of range. Adjacency is computed once when the map loads, so walking it is cheap."},
    {"provinceIsCoastal", gbxpy_province_is_coastal, METH_VARARGS, "Whether a province touches water. Ports, embarking and naval bombardment all require it."},
    {"seaRouteExists", gbxpy_sea_route_exists, METH_VARARGS, "Whether a fleet could get from one point to another by sea, using the game's own navigation grid. You cannot compute this from province neighbours: those describe LAND adjacency."},
    {"pointIsLand", gbxpy_point_is_land, METH_VARARGS, "Whether a world coordinate is land. Ordering a ship onto land is not an error -- the resolver clamps it -- but knowing first is cheaper."},
#endif /* GBX_WITH_MAP */

/* ---- MapEditor (20) ---- */
#if GBX_WITH_MAPEDITOR
    {"editorActive", gbxpy_editor_active, METH_NOARGS, "Whether the map editor is open with a project loaded. EVERY OTHER CALL IN THIS MODULE returns 0 or an empty string when this is 0, including from inside a running game: the data be"},
    {"editorProvinceCount", gbxpy_editor_province_count, METH_NOARGS, "How many provinces the open project has. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvinceAt", gbxpy_editor_province_at, METH_VARARGS, "The province id at `index`, in ascending id order, or 0xFFFFFFFF past the end. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvincePopulation", gbxpy_editor_province_population, METH_VARARGS, "Population. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvinceIndustryLevel", gbxpy_editor_province_industry_level, METH_VARARGS, "Industry level, 0..10. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvinceFortification", gbxpy_editor_province_fortification, METH_VARARGS, "Fortification, 0..5. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvincePortLevel", gbxpy_editor_province_port_level, METH_VARARGS, "Port level, 0..3. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvinceResource", gbxpy_editor_province_resource, METH_VARARGS, "Resource amount, 0..100. `which` is \"oil\", \"gold\", \"rubber\", \"gemstones\" or \"metal\". Returns a neutral value unless the map editor is open with a project loaded -- see ma"},
    {"editorProvinceCompassEcon", gbxpy_editor_province_compass_econ, METH_VARARGS, "Province economic compass, -100..100. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorProvinceCompassSocial", gbxpy_editor_province_compass_social, METH_VARARGS, "Province social compass, -100..100. Returns a neutral value unless the map editor is open with a project loaded -- see mapeditor/active."},
    {"editorSetProvincePopulation", gbxpy_editor_set_province_population, METH_VARARGS, "Set population, clamped to 0..2e9. Writes the SAME per-province data the editor's own tools write, so it saves, exports and shows up in the unsaved-changes prompt like any other ed"},
    {"editorSetProvinceIndustryLevel", gbxpy_editor_set_province_industry_level, METH_VARARGS, "Set industry level, clamped to 0..10. Writes the SAME per-province data the editor's own tools write, so it saves, exports and shows up in the unsaved-changes prompt like any other"},
    {"editorSetProvinceFortification", gbxpy_editor_set_province_fortification, METH_VARARGS, "Set fortification, clamped to 0..5. Writes the SAME per-province data the editor's own tools write, so it saves, exports and shows up in the unsaved-changes prompt like any other e"},
    {"editorSetProvincePortLevel", gbxpy_editor_set_province_port_level, METH_VARARGS, "Set port level, clamped to 0..3. Writes the SAME per-province data the editor's own tools write, so it saves, exports and shows up in the unsaved-changes prompt like any other edit"},
    {"editorSetProvinceResource", gbxpy_editor_set_province_resource, METH_VARARGS, "Set a resource amount, clamped to 0..100. An unrecognised name is refused rather than silently mapped onto oil. Writes the SAME per-province data the editor's own tools write, so i"},
    {"editorSetProvinceCompass", gbxpy_editor_set_province_compass, METH_VARARGS, "Set both compass axes, each clamped to -100..100. Writes the SAME per-province data the editor's own tools write, so it saves, exports and shows up in the unsaved-changes prompt li"},
    {"editorMapName", gbxpy_editor_map_name, METH_NOARGS, "The project's map name. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap."},
    {"editorSetMapName", gbxpy_editor_set_map_name, METH_VARARGS, "Rename the map. Refused if empty or over 96 bytes."},
    {"editorSetAuthor", gbxpy_editor_set_author, METH_VARARGS, "Set the author recorded in the exported .odmap. Up to 96 bytes."},
    {"editorSetLicense", gbxpy_editor_set_license, METH_VARARGS, "Set the licence recorded in the exported .odmap. Up to 96 bytes."},
#endif /* GBX_WITH_MAPEDITOR */

/* ---- Military.Read (20) ---- */
#if GBX_WITH_MILITARY_READ
    {"shipCount", gbxpy_ship_count, METH_NOARGS, "How many ships exist in the world, across all owners."},
    {"shipAt", gbxpy_ship_at, METH_VARARGS, "The ship id at `index` in 0..ship_count-1, or 0xFFFFFFFF past the end. Ids are stable within a turn and not across turns -- do not store one."},
    {"shipExists", gbxpy_ship_exists, METH_VARARGS, "Whether a ship id is still live. Check this before acting on an id you read earlier in the same turn; ships sink."},
    {"shipOwner", gbxpy_ship_owner, METH_VARARGS, "The country that owns a ship, or 0xFFFFFFFF for an id that does not exist."},
    {"shipType", gbxpy_ship_type, METH_VARARGS, "The hull type as a lowercase string: \"transport\", \"destroyer\", \"battleship\", \"carrier\", \"submarine\". Two-call sizing: call with cap 0 to learn the length, allocate, call "},
    {"shipLon", gbxpy_ship_lon, METH_VARARGS, "Longitude in degrees, -180..180. Ships live in world coordinates, not provinces."},
    {"shipLat", gbxpy_ship_lat, METH_VARARGS, "Latitude in degrees, -90..90."},
    {"shipHealth", gbxpy_ship_health, METH_VARARGS, "Hull integrity, 0..100. A ship at 0 has already sunk and will not appear."},
    {"shipCrew", gbxpy_ship_crew, METH_VARARGS, "Crew aboard. For a transport this includes the embarked army, which is why a sunk transport costs so much more than its hull."},
    {"shipRange", gbxpy_ship_range, METH_VARARGS, "How far this hull may move in one turn, in degrees. The resolver clamps any order beyond it, so read this before ordering a move rather than discovering the clamp afterwards."},
    {"armyStackCount", gbxpy_army_stack_count, METH_VARARGS, "How many distinct owners have troops in a province. Usually 1; more than one means a contested or garrisoned province."},
    {"armyStackOwner", gbxpy_army_stack_owner, METH_VARARGS, "The country owning stack `index` in a province, or 0xFFFFFFFF past the end."},
    {"armyStackSize", gbxpy_army_stack_size, METH_VARARGS, "How many troops are in that stack."},
    {"countryArmy", gbxpy_country_army, METH_VARARGS, "A country's total troops everywhere, which is the number its own army screen shows."},
    {"provinceFortification", gbxpy_province_fortification, METH_VARARGS, "Fortification level, 0..5. Multiplies the defender's strength."},
    {"provincePortLevel", gbxpy_province_port_level, METH_VARARGS, "Port level, 0..3. 0 means no port, so no embarking and no ship repair."},
    {"troopTypeCount", gbxpy_troop_type_count, METH_NOARGS, "How many kinds of soldier exist."},
    {"troopTypeId", gbxpy_troop_type_id, METH_VARARGS, "The stable id of troop type `index` -- line, militia, assault, mech. Never translated. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full "},
    {"countryArmyOfType", gbxpy_country_army_of_type, METH_VARARGS, "How many soldiers of that kind this country has, everywhere. 0 for a troop type that does not exist."},
    {"provinceTroopsOfType", gbxpy_province_troops_of_type, METH_VARARGS, "How many soldiers of that kind this country has standing in that province."},
#endif /* GBX_WITH_MILITARY_READ */

/* ---- Military.Write (4) ---- */
#if GBX_WITH_MILITARY_WRITE
    {"orderArmyMove", gbxpy_order_army_move, METH_VARARGS, "Move `percent` (0..100) of the troops in `from` into the adjacent province `to`. Into an enemy province this is an attack; into your own or an ally's it is a transfer. Non-adjacent"},
    {"orderShipMove", gbxpy_order_ship_move, METH_VARARGS, "Sail a ship toward (lon,lat). The resolver routes around land and clamps to ship_range, so a destination on land or beyond range moves the ship as far as it legally can rather than"},
    {"orderShipEngage", gbxpy_order_ship_engage, METH_VARARGS, "Attack another ship. Requires that you are at war with its owner and that it is within range; both are checked by the resolver. QUEUES AN ORDER; it does not move anything. It lands"},
    {"orderShipBombard", gbxpy_order_ship_bombard, METH_VARARGS, "Bombard a coastal province. `ammo` names the shell type; pass an empty string for the default. QUEUES AN ORDER; it does not move anything. It lands in the same queue the player's o"},
#endif /* GBX_WITH_MILITARY_WRITE */

/* ---- Net (7) ---- */
#if GBX_WITH_NET
    {"send", gbxpy_send, METH_VARARGS, "Send a message to the same mod running on another peer. `peer` is a peer id -- the value `recv` reported in `from_peer` -- and -1 broadcasts to every other peer, the host included."},
    {"peerCount", gbxpy_peer_count, METH_NOARGS, "How many players this session has, a playing host included. 0 when this is not a network game, which is how a mod tells the difference. Spectators are not counted."},
    {"selfPeer", gbxpy_self_peer, METH_NOARGS, "This machine's own peer id. 0 means this is not a network game, or this is a dedicated host holding no seat -- a host that plays has an ordinary peer id like anyone else, so do not"},
    {"isHost", gbxpy_is_host, METH_NOARGS, "Whether this copy is the authoritative one. A mod that computes anything the game depends on must do it here and send the result, not compute it separately on each machine."},
    {"peerAt", gbxpy_peer_at, METH_VARARGS, "The peer id at `index` in 0..peer_count-1, or 0xFFFFFFFF past the end. This is the id net/send takes."},
    {"peerName", gbxpy_peer_name, METH_VARARGS, "That peer's display name -- deliberately NOT their account id or issuer. A mod has no business correlating players across sessions. Two-call sizing: call with cap 0 to learn the le"},
    {"maxMessageBytes", gbxpy_max_message_bytes, METH_NOARGS, "The largest payload net/send will accept. Chunk against this rather than discovering the limit by having a message dropped."},
#endif /* GBX_WITH_NET */

/* ---- Neural (16) ---- */
#if GBX_WITH_NEURAL
    {"featureCount", gbxpy_feature_count, METH_NOARGS, "How many floats are in the AI's feature vector. 0 when there is no AI or no world."},
    {"features", gbxpy_features, METH_VARARGS, "Copies the feature vector the AI would see for a country, as 32-bit floats. Two-call sizing, but note cap counts FLOATS and the buffer must therefore be cap*4 bytes. This is a snap"},
    {"rewardCount", gbxpy_reward_count, METH_NOARGS, "How many reward channels the AI tracks (economy, politics, war, navy)."},
    {"rewardMean", gbxpy_reward_mean, METH_VARARGS, "The running mean reward for one channel, indexed in [0, reward_count). 0 if out of range. OBSERVE ONLY: this capability has no import that writes to the model, the optimiser state "},
    {"moduleCount", gbxpy_module_count, METH_NOARGS, "How many decision modules the AI has. Each acts independently every turn."},
    {"moduleName", gbxpy_module_name, METH_VARARGS, "The module's name: \"economy\", \"politics\", \"war\", \"navy\". Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the"},
    {"actionCount", gbxpy_action_count, METH_VARARGS, "How many actions that module can choose between."},
    {"actionName", gbxpy_action_name, METH_VARARGS, "The action's name, e.g. \"reinforce\", \"embark\", \"propose_alliance\". THE FEATURE VECTOR IS DELIBERATELY NOT NAMED: its 143 slots are an implementation detail that has changed b"},
    {"countryIsAi", gbxpy_country_is_ai, METH_VARARGS, "Whether a country is played by the AI rather than by the local player."},
    {"updateCount", gbxpy_update_count, METH_NOARGS, "Gradient updates the loaded model has been through -- roughly, how much training it has seen."},
    {"modelLoaded", gbxpy_model_loaded, METH_NOARGS, "Whether an AI model is loaded at all. False in a game with no AI players."},
    {"aiVersion", gbxpy_ai_version, METH_NOARGS, "The AI's own version, e.g. \"ParrotZero 8.4.0\" -- ARCH.RULES.PATCH, and independent of the game's version. ARCH is the network shape and action space, RULES is behaviour a benchma"},
    {"aiArch", gbxpy_ai_arch, METH_NOARGS, "The AI's ARCH number on its own, which is also the model file's format byte. The feature count and the action sets are only stable within one ARCH; a bump means old weights are ref"},
    {"countryStance", gbxpy_country_stance, METH_VARARGS, "The posture the AI has chosen for this country -- 0 expand, 1 consolidate, 2 defend, 3 develop -- or GEARBOX_INVALID if it holds none (a country the AI does not play, or one that h"},
    {"stanceName", gbxpy_stance_name, METH_VARARGS, "The stance's name: \"expand\", \"consolidate\", \"defend\", \"develop\". Never translated, and stable within an ARCH. Two-call sizing: call with cap 0 to learn the length, allocate"},
    {"stanceCount", gbxpy_stance_count, METH_NOARGS, "How many stances there are to choose between."},
#endif /* GBX_WITH_NEURAL */

/* ---- Neural.Decide (1) ---- */
#if GBX_WITH_NEURAL_DECIDE
    {"actionValid", gbxpy_action_valid, METH_VARARGS, "Which actions the host will accept for this module right now, one byte per action: 1 legal, 0 not. Two-call sizing, like every other copy here. MEANINGFUL ONLY INSIDE mod_ai_choose"},
#endif /* GBX_WITH_NEURAL_DECIDE */

/* ---- Politics.Read (29) ---- */
#if GBX_WITH_POLITICS_READ
    {"countryCompassEcon", gbxpy_country_compass_econ, METH_VARARGS, "Economic axis of the political compass, -100 (planned) to 100 (market)."},
    {"countryCompassSocial", gbxpy_country_compass_social, METH_VARARGS, "Social axis, -100 (authoritarian) to 100 (libertarian)."},
    {"provinceUnrest", gbxpy_province_unrest, METH_VARARGS, "This province's chance of rebelling, as the game itself computes it."},
    {"policyCount", gbxpy_policy_count, METH_NOARGS, "How many policies exist."},
    {"policyId", gbxpy_policy_id, METH_VARARGS, "The stable string id of policy `index`. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap"},
    {"policyName", gbxpy_policy_name, METH_VARARGS, "The policy's display name; localised, not stable, do not match on it. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way"},
    {"countryHasPolicy", gbxpy_country_has_policy, METH_VARARGS, "Whether a country currently has a policy active or implementing."},
    {"provinceMinorityCount", gbxpy_province_minority_count, METH_VARARGS, "How many named minority groups live in a province."},
    {"provinceMinorityName", gbxpy_province_minority_name, METH_VARARGS, "The minority's name. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap."},
    {"provinceMinorityShare", gbxpy_province_minority_share, METH_VARARGS, "That minority's share of the province's population, 0..1."},
    {"countryDistrictCount", gbxpy_country_district_count, METH_VARARGS, "How many districts this country is divided into. Districts are built on demand, so asking is what creates the default one for a country that has never been divided."},
    {"countryDistrictName", gbxpy_country_district_name, METH_VARARGS, "The district's name. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap."},
    {"countryDistrictShare", gbxpy_country_district_share, METH_VARARGS, "This district's claim on the country's pacification budget, in percent. The shares of a country's districts sum to 100."},
    {"countryDistrictProvinceCount", gbxpy_country_district_province_count, METH_VARARGS, "How many provinces this district holds."},
    {"countryDistrictProvince", gbxpy_country_district_province, METH_VARARGS, "Province `n` of this district, or GEARBOX_INVALID if there is no such one."},
    {"countryDistrictLawCount", gbxpy_country_district_law_count, METH_VARARGS, "How many regional laws this district runs."},
    {"countryDistrictLaw", gbxpy_country_district_law, METH_VARARGS, "The stable id of regional law `n` in this district. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is trun"},
    {"districtLawCount", gbxpy_district_law_count, METH_NOARGS, "How many regional laws exist to choose from."},
    {"districtLawId", gbxpy_district_law_id, METH_VARARGS, "The stable id of regional law `index`. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap."},
    {"districtLawName", gbxpy_district_law_name, METH_VARARGS, "The display name of regional law `index`, untranslated. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is "},
    {"countryDiscloses", gbxpy_country_discloses, METH_VARARGS, "Whether this country publishes that figure in its profile: 1 if it does, 0 if it keeps it to itself. See the disclosure_field enum. Publishing is a decision with a consequence -- m"},
    {"countryPartyCount", gbxpy_country_party_count, METH_VARARGS, "How many parties sit in a country's legislature. 0 when the party rules are off, which is the default -- so a mod must treat 0 as 'this world has no party politics' rather than as "},
    {"countryPartyName", gbxpy_country_party_name, METH_VARARGS, "The party's name. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap."},
    {"countryPartyShortName", gbxpy_country_party_short_name, METH_VARARGS, "The party's abbreviation, for a list that has to fit -- \"SPD\", \"INC\". Same two-call sizing as country_party_name. May be empty."},
    {"countryPartySupport", gbxpy_country_party_support, METH_VARARGS, "That party's share of the country, 0..1. The shares of one country's parties are a partition and sum to 1, so they may be compared directly but must never be added across countries"},
    {"countryPartyCompassEcon", gbxpy_country_party_compass_econ, METH_VARARGS, "Where the party stands on the economic axis, -100 (planned) to 100 (market) -- the same axis and scale as country_compass_econ, so the distance between a party and its government i"},
    {"countryPartyCompassSocial", gbxpy_country_party_compass_social, METH_VARARGS, "Where the party stands on the social axis, -100 (authoritarian) to 100 (libertarian). Same scale as country_compass_social."},
    {"countryPartyIsHistorical", gbxpy_country_party_is_historical, METH_VARARGS, "1 when this party is a matter of record for the scenario's date -- it existed, under this name -- and 0 when the name was generated from its stance. A mod that displays party names"},
    {"countryRulingParty", gbxpy_country_ruling_party, METH_VARARGS, "The index of the party that governs, or -1 if none does. That party pulls the government compass toward its own stance every turn it holds power, which is why the two are on the sa"},
#endif /* GBX_WITH_POLITICS_READ */

/* ---- Politics.Write (4) ---- */
#if GBX_WITH_POLITICS_WRITE
    {"setCountryPolicy", gbxpy_set_country_policy, METH_VARARGS, "Enact or cancel a policy. GOES THROUGH THE GAME'S OWN enactPolicy, so the cost, the prerequisites and the per-turn enactment cap all still apply -- a country cannot end up running "},
    {"setCountryDistrictShare", gbxpy_set_country_district_share, METH_VARARGS, "Set this district's claim on the pacification budget. The other districts are rebalanced so the shares still sum to 100, exactly as dragging the slider does. Returns 1 on success."},
    {"setCountryDistrictLaw", gbxpy_set_country_district_law, METH_VARARGS, "Pass or repeal a regional law in this district. Returns 1 on success, 0 for an unknown law or district."},
    {"setCountryDisclosure", gbxpy_set_country_disclosure, METH_VARARGS, "Publish or withhold one of the figures in this country's profile. Returns 1 on success."},
#endif /* GBX_WITH_POLITICS_WRITE */

/* ---- Render (5) ---- */
#if GBX_WITH_RENDER
    {"provinceTint", gbxpy_province_tint, METH_VARARGS, "Tint a province on the map. rgba is 0xRRGGBBAA.  AN ALPHA OF ZERO REMOVES THE TINT. One call does set and clear, so a fading effect that paints transparent every frame cannot grow "},
    {"provinceLabel", gbxpy_province_label, METH_VARARGS, "Put a short label at a province. Empty text, or an alpha of zero, removes it.  Truncated at 48 characters and stripped of control characters rather than refused: a label one charac"},
    {"renderClear", gbxpy_render_clear, METH_NOARGS, "Drop every tint and label YOU have drawn. A mod cannot clear another mod's -- and unloading a mod clears its own automatically, because a mark left behind by a mod that is no longe"},
    {"tintCount", gbxpy_tint_count, METH_NOARGS, "How many tints you are currently holding."},
    {"labelCount", gbxpy_label_count, METH_NOARGS, "How many labels you are currently holding."},
#endif /* GBX_WITH_RENDER */

/* ---- Research.Read (8) ---- */
#if GBX_WITH_RESEARCH_READ
    {"nodeCount", gbxpy_node_count, METH_NOARGS, "How many technologies exist in the tree."},
    {"nodeId", gbxpy_node_id, METH_VARARGS, "The stable string id of technology `index`, which is what country_has_researched takes. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full"},
    {"nodeName", gbxpy_node_name, METH_VARARGS, "The technology's display name, which is localised and NOT stable -- never match on it. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full "},
    {"nodeCategory", gbxpy_node_category, METH_VARARGS, "Which branch of the tree it sits in. Two-call sizing: call with cap 0 to learn the length, allocate, call again. Returns the full length either way; the copy is truncated to cap."},
    {"nodeCost", gbxpy_node_cost, METH_VARARGS, "Research points required."},
    {"countryHasResearched", gbxpy_country_has_researched, METH_VARARGS, "Whether a country has completed a technology. Takes the id from node_id, not the display name."},
    {"countryFunding", gbxpy_country_funding, METH_VARARGS, "Research funding as A SHARE OF INCOME, 0..1 -- not an absolute sum. That is how the game stores it and how its own economy screen presents it."},
    {"countryResearchGroups", gbxpy_country_research_groups, METH_VARARGS, "How many research programmes this country may run at once, 1 to 3. This is the effective number, including any override a script or a mod has set."},
#endif /* GBX_WITH_RESEARCH_READ */

/* ---- Research.Write (2) ---- */
#if GBX_WITH_RESEARCH_WRITE
    {"setCountryFunding", gbxpy_set_country_funding, METH_VARARGS, "Set research funding as a share of income. Clamped to 0..1; a value in 'points per turn' is not a quantity this game has."},
    {"setCountryResearchGroups", gbxpy_set_country_research_groups, METH_VARARGS, "Force how many research programmes a country may run, 1 to 3, or 0 to hand the decision back to its economy. Outranks the economic gate in both directions and is saved with the gam"},
#endif /* GBX_WITH_RESEARCH_WRITE */

/* ---- Scripts (6) ---- */
#if GBX_WITH_SCRIPTS
    {"commandAdd", gbxpy_command_add, METH_VARARGS, "Claim a command name in the map script language. A script line beginning with it is then handed to your mod_script_command export, whole.  FIRST COME. A script writes `reinforce FR"},
    {"commandRemove", gbxpy_command_remove, METH_VARARGS, "Give up one of your own commands. False if it was not yours -- a mod cannot unregister another mod's."},
    {"commandCount", gbxpy_command_count, METH_NOARGS, "How many commands YOU have claimed."},
    {"commandName", gbxpy_command_name, METH_VARARGS, "The name of your command at index, sorted. Two-call sizing."},
    {"commandText", gbxpy_command_text, METH_NOARGS, "Inside mod_script_command: which of your commands the script ran. Empty outside that call -- there is no command then, and reporting the last one would be a stale answer that looks"},
    {"commandArgs", gbxpy_command_args, METH_NOARGS, "Inside mod_script_command: the rest of the script line, verbatim -- unparsed and untrimmed, because your command knows its own grammar and the engine does not. Empty outside that c"},
#endif /* GBX_WITH_SCRIPTS */

/* ---- Storage (3) ---- */
#if GBX_WITH_STORAGE
    {"get", gbxpy_get, METH_VARARGS, "Reads one of your own keys. Two-call sizing: returns the full value length and writes at most cap bytes. Returns GEARBOX_INVALID if the key is absent -- which is NOT the same as a "},
    {"set", gbxpy_set, METH_VARARGS, "Stores bytes under one of your own keys. Returns 1 on success, 0 if a quota was exceeded (256 keys, 128-byte keys, 16 KiB per value, 64 KiB total per mod) -- the reason is written "},
    {"remove", gbxpy_remove, METH_VARARGS, "Deletes one of your own keys. Returns 1 if it existed, 0 if it did not."},
#endif /* GBX_WITH_STORAGE */

/* ---- UI (17) ---- */
#if GBX_WITH_UI
    {"panelRegister", gbxpy_panel_register, METH_VARARGS, "Register a panel and return its handle. Returns 0 (invalid) when headless, when UI was revoked, or when you already hold 8 panels. Titles are truncated to 64 bytes. Call this from "},
    {"drawRect", gbxpy_draw_rect, METH_VARARGS, "Filled rectangle in panel-relative coordinates. Colour is 0xRRGGBBAA. Coordinates outside the panel are clipped by the host; they cannot escape it."},
    {"drawText", gbxpy_draw_text, METH_VARARGS, "UTF-8 text in panel-relative coordinates. Truncated to 512 bytes per call."},
    {"button", gbxpy_button, METH_VARARGS, "Immediate-mode button: draws it and returns 1 on the frame it is clicked. One click activates one button -- the host consumes it, so overlapping rects do not all fire. Label trunca"},
    {"drawLine", gbxpy_draw_line, METH_VARARGS, "Queue a line from (x1,y1) to (x2,y2) in panel-relative pixels. Thickness is clamped to 0.25..64. Clipped to your panel like every other command."},
    {"drawCircle", gbxpy_draw_circle, METH_VARARGS, "Queue a filled circle centred at (cx,cy), panel-relative. Radius is clamped to 0..4096."},
    {"drawImage", gbxpy_draw_image, METH_VARARGS, "Queue an image from YOUR OWN package -- `name` is a path inside your .odmod, resolved exactly as gearbox:assets/read resolves it, so you cannot name a file on disk, a game asset, o"},
    {"drawTextSized", gbxpy_draw_text_sized, METH_VARARGS, "Like draw_text but with a type size, clamped to 6..96. draw_text remains 14pt, unchanged, so v1.0 mods look exactly as they did."},
    {"measureText", gbxpy_measure_text, METH_VARARGS, "Width in pixels of `text` at `size`, measured with the font the game will actually draw. Centring, right-alignment and wrapping all need this before the text is queued."},
    {"panelWidth", gbxpy_panel_width, METH_VARARGS, "The width the host assigned your panel this frame, in pixels. Lay out against this rather than against min_w -- the host may have given you more."},
    {"panelHeight", gbxpy_panel_height, METH_VARARGS, "The height the host assigned your panel this frame, in pixels."},
    {"panelSetVisible", gbxpy_panel_set_visible, METH_VARARGS, "Show or hide one of your panels. A hidden panel is not drawn and receives no input, but keeps its handle and its registration."},
    {"mouseX", gbxpy_mouse_x, METH_VARARGS, "Cursor X, panel-relative, or 0 when the cursor is not over your panel. You cannot observe the pointer outside your own box."},
    {"mouseY", gbxpy_mouse_y, METH_VARARGS, "Cursor Y, panel-relative, or 0 when the cursor is not over your panel."},
    {"mouseInside", gbxpy_mouse_inside, METH_VARARGS, "Whether the cursor is over your panel this frame."},
    {"themeAccent", gbxpy_theme_accent, METH_NOARGS, "The PLAYER's accent colour as 0x00RRGGBB -- not another mod's override. Build your palette around this and you harmonise with what they chose."},
    {"setThemeAccent", gbxpy_set_theme_accent, METH_VARARGS, "Restyle the whole interface. The accent is read at over a hundred sites -- every heading, highlight, selection and button -- so this is the cheapest full reskin there is. It is NOT"},
#endif /* GBX_WITH_UI */
